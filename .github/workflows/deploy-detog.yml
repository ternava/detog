name: Zola on GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    name: Publish site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4

      - name: Ensure data directory exists
        run: mkdir -p static/data

      - name: Extract feature-gate events
        run: |
          chmod +x scripts/clone-extract-feature-gates.sh
          scripts/clone-extract-feature-gates.sh

      - name: Debug clone output
        run: |
          echo "Directory tree under code:"
          ls -R code || echo "No code directory"
          echo "--- CSV preview ---"
          head -n 10 static/data/feature_gates_events.csv || echo "CSV not found"

      - name: Build and deploy
        uses: shalzz/zola-deploy-action@v0.21.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}